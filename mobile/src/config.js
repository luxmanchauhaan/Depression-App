// IMPORTANT: "localhost" only works from a simulator running on the same
// machine as the backend. On a real phone (Expo Go), you MUST use your
// computer's local network IP instead, e.g. "http://192.168.1.42:4000".
//
// Find your IP on Windows: open Command Prompt, run `ipconfig`,
// look for "IPv4 Address" under your active Wi-Fi adapter.

export const API_URL = 'http://172.17.4.225:4000';
